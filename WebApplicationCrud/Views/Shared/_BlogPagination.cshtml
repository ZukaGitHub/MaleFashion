@model IndexViewModel

<div class="pages">
    <div class="paginationBlog">
       
        <a asp-action="Blog" asp-controller="Home" class="previous-page" asp-route-category="@Model.Category"
           asp-route-search="@Model.Search" asp-route-pageNumber="@(Model.PageNumber - 1)">Previous </a>

        @foreach (var p in Model.Pages)
        {
            if (p == -1)
            {
    <span class="three-dots">
        ...
    </span> }
else
{
    <a class="@(p == Model.PageNumber ? "currentPage" : "") activePage" asp-action="Blog" asp-controller="Home" asp-route-category="@Model.Category"
       asp-route-search="@Model.Search" asp-route-pageNumber="@p">@p</a>}
}

        <a asp-action="Blog" asp-controller="Home" class="next-page" asp-route-category="@Model.Category"
           asp-route-search="@Model.Search" asp-route-pageNumber="@(Model.PageNumber + 1)">Next</a>

    
    </div>
</div>